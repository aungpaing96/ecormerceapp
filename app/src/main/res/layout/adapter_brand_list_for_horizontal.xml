<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:id="@+id/cl"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:background="@drawable/cl_brand_horizontal_bg"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        android:layout_marginStart="@dimen/label_margin_start_end">

        <ImageView
            android:id="@+id/iv_brand"
            android:layout_width="@dimen/iv_brand_horizontal_width"
            android:layout_height="@dimen/iv_brand_horizontal_height"
            android:src="@drawable/ic_nike"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintStart_toStartOf="parent"/>

    </androidx.constraintlayout.widget.ConstraintLayout>

    <View
        android:id="@+id/view_fake"
        android:layout_width="@dimen/label_margin_start_end"
        android:layout_height="@dimen/iv_size_height"
        android:visibility="gone"
        app:layout_constraintTop_toTopOf="@id/cl"
        app:layout_constraintStart_toEndOf="@id/cl"/>

</androidx.constraintlayout.widget.ConstraintLayout>