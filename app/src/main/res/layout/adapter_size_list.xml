<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <TextView
        android:id="@+id/tv_size"
        android:layout_width="@dimen/iv_size_width"
        android:layout_height="@dimen/iv_size_height"
        style="@style/TextValueStyle"
        android:gravity="center"
        android:text="5"
        android:background="@drawable/tv_size_bg_selected"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        android:layout_marginStart="@dimen/label_margin_start_end"
        android:paddingTop="5dp"/>

    <View
        android:id="@+id/view_fake"
        android:layout_width="@dimen/label_margin_start_end"
        android:layout_height="@dimen/iv_size_height"
        android:visibility="gone"
        app:layout_constraintTop_toTopOf="@id/tv_size"
        app:layout_constraintStart_toEndOf="@id/tv_size"/>

</androidx.constraintlayout.widget.ConstraintLayout>